name: Event Scraper Automation

on:
  schedule:
    # Läuft jeden Montag um 04:00 Uhr UTC
    - cron: '0 4 * * 1'
  workflow_dispatch: # Erlaubt auch manuelles Starten im GitHub Dashboard

jobs:
  build-and-scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Code auschecken
        uses: actions/checkout@v3

      - name: Python 3.10 einrichten
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Abhängigkeiten installieren
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Scraper ausführen
        run: python scraper.py

      - name: Neue events.json committen und pushen
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action Bot"
          git add events.json
          git commit -m "Auto-update: Neue Triathlon Events gescrapt" || exit 0
          git push
